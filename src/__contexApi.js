/*
1.Create Context (export)
2.Provider
3.set value
4.useContext
*/

// AuthProvider

/*
1.create context : export const AuthContext =createContext (null)  || also export it 

2.set provider with value : <AuthContext.Provider value={something}></AuthContext.Provider>

3.use the Auth Provider in the main.jsx file

4.access children in the AuthProvider component as children and use it in the middle of the Provider 
*/
